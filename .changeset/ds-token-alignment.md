---
"@repo/ui": patch
"@repo/config": patch
---

Migrate all UI primitives from shadcn bridge variables to direct --ds-\* design token references and add accent color token suite (9 hues x 7 token types)
