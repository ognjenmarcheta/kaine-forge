---
"@repo/api": patch
---

Prove organization scoping by executing GraphQL operations over real HTTP against an in-process Postgres (PGlite), covering the list query, the point read, a cross-organization update, the batched todos DataLoader, the membership gate, and the unauthenticated path.
