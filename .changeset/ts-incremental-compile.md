---
"@repo/config": patch
---

Enable TypeScript incremental compilation (`.tsbuildinfo`) in the shared package base config without turning on project-reference `composite` (still deferred — issue #147).
