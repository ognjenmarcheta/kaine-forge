name: PR Labeler

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize
      - reopened
      - ready_for_review

permissions:
  contents: read
  pull-requests: write

concurrency:
  # pull_request_target resolves github.ref to the base branch (main), so a
  # ref-keyed group cancels labeling across every open PR. Key on PR number.
  group: labeler-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  label:
    name: Apply PR labels
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Apply file-based labels
        uses: actions/labeler@bf12e9b00b37c5c0ca2b87b79b2daf7891dbda13 # v7.0.0
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          configuration-path: .github/labeler.yml
          sync-labels: false

      - name: Apply type label from PR title
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const title = context.payload.pull_request?.title ?? "";
            const number = context.payload.pull_request?.number;

            if (!number) {
              core.info("No PR number available.");
              return;
            }

            const typePatterns = [
              [/^build(\(.+\))?:/i, "type:build"],
              [/^chore(\(.+\))?:/i, "type:chore"],
              [/^ci(\(.+\))?:/i, "type:ci"],
              [/^docs(\(.+\))?:/i, "type:docs"],
              [/^feat(\(.+\))?:/i, "type:feat"],
              [/^fix(\(.+\))?:/i, "type:fix"],
              [/^perf(\(.+\))?:/i, "type:perf"],
              [/^refactor(\(.+\))?:/i, "type:refactor"],
              [/^revert(\(.+\))?:/i, "type:revert"],
              [/^style(\(.+\))?:/i, "type:style"],
              [/^test(\(.+\))?:/i, "type:test"]
            ];

            const matchedType = typePatterns.find(([pattern]) => pattern.test(title))?.[1];
            const { data: currentLabels } = await github.rest.issues.listLabelsOnIssue({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: number
            });

            const currentNames = currentLabels.map((label) => label.name);
            const staleTypeLabels = currentNames.filter(
              (name) => name.startsWith("type:") && name !== matchedType
            );

            for (const label of staleTypeLabels) {
              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: number,
                  name: label
                });
              } catch (error) {
                core.warning(`Could not remove stale label ${label}: ${error.message}`);
              }
            }

            if (matchedType && !currentNames.includes(matchedType)) {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: number,
                labels: [matchedType]
              });
            }
